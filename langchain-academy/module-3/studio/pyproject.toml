[project]
name = "studio"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "langgraph",
    "langgraph-prebuilt",
    "langchain-core",
    "langchain-community",
    "langchain-openai",
]

[tool.setuptools]
py-modules = ["agent", "dynamic_breakpoints"]
