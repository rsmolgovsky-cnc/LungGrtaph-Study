[project]
name = "deployment"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "langgraph",
    "langgraph-prebuilt",
    "langchain-core",
    "langchain-community",
    "langchain-openai",
    "trustcall",
]

[tool.setuptools]
py-modules = ["task_maistro"]
