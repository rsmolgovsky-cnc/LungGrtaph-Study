[project]
name = "studio"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "langgraph",
    "langgraph-prebuilt",
    "langchain-core",
    "langchain-community",
    "langchain-openai",
    "langchain-tavily",
    "wikipedia",
]

[tool.setuptools]
py-modules = ["parallelization", "sub_graphs", "map_reduce", "research_assistant"]
